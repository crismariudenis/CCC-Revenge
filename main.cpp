#include "Program.h"

signed main()
{
    cin >> nrLines;
    while (nrLines)
    {
        string code;
        cin >> code;
        nrLines--;
        if (code == "start")
            Program p;
    }
}