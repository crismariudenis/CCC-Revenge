#include "Program.h"

signed main()
{
    cin >> nrLines;
    while (nrLines)
    {
        string token;
        cin >> token;
        nrLines--;
        if (token == "start")
            Program p;
    }
}